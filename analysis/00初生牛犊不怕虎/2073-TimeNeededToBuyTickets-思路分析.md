# 2073. 买票需要的时间 (Time Needed to Buy Tickets)

## 📋 题目信息
- **难度**：简单 (Easy)
- **标签**：数组 (Array)、模拟 (Simulation)、队列 (Queue)
- **来源**：LeetCode

## 📖 题目描述

有 `n` 个人前来排队买票，其中第 `0` 人站在队伍 **最前方**，第 `(n - 1)` 人站在队伍 **最后方**。

给你一个下标从 **0** 开始的整数数组 `tickets`，数组长度为 `n`，其中第 `i` 人想要购买的票数为 `tickets[i]`。

每个人买票都需要用掉 **恰好 1 秒**。一个人 **一次只能买一张票**，如果需要购买更多票，他必须走到 **队尾** 重新排队（**瞬间**发生，不计时间）。如果一个人没有剩下需要买的票，那他将会 **离开** 队伍。

返回位于位置 `k`（下标从 **0** 开始）的人完成买票需要的时间（以秒为单位）。

### 示例

**示例 1：**
```
输入：tickets = [2,3,2], k = 2
输出：6
解释：
- 第1秒：[2,3,2] -> 0号买1张，变[1,3,2]，去队尾
- 第2秒：[1,3,2] -> 1号买1张，变[1,2,2]，去队尾
- 第3秒：[1,2,2] -> 2号(目标)买1张，变[1,2,1]，去队尾
- 第4秒：[1,2,1] -> 0号买1张，变[0,2,1]，离开
- 第5秒：[0,2,1] -> 1号买1张，变[0,1,1]，去队尾
- 第6秒：[0,1,1] -> 2号(目标)买1张，变[0,1,0]，离开。总耗时6秒。
```

**示例 2：**
```
输入：tickets = [5,1,1,1], k = 0
输出：8
```

### 约束条件
- `n == tickets.length`
- `1 <= n <= 100`
- `1 <= tickets[i] <= 100`
- `0 <= k < n`

---

## 🤔 题目分析

### 问题理解
这道题描述了一个简单的**轮转队列**过程。
1.  每人轮流买 1 张票。
2.  买完如果不满足需求，去队尾继续排。
3.  我们关注的是队列中**特定位置 k** 的那个人，他什么时候能买够票离开。

### 关键观察
- **时间 = 总购买次数**：因为买一张票耗时 1 秒，所以题目求的“时间”其实就是“所有人在 k 离开前总共买了多少张票”。
- **相对位置很重要**：
    - 队伍可以看作在不断循环。
    - **排在 k 前面的人**：在每一轮循环中，如果 k 能买到票，他们一定也能买到（除非他们已经买够离开了）。
    - **排在 k 后面的人**：在 k 买完最后一张票离开的那一轮（最后一轮），排在 k 后面的人是没有机会买票的。

---

## 💡 解题思路

### 方法一：直接模拟（暴力法）

#### 🌟 形象化理解：发扑克牌

**场景类比**：
想象你在玩发牌游戏。
- `tickets` 数组就是每个人手里需要的牌数。
- 你（发牌员）按座位顺序每人发一张牌。
- 如果某人还要牌，你就发一张，计数器+1。
- 如果某人不要了，你就跳过他。
- 你的目标是：当**第 k 个人**拿到他需要的最后一张牌时，你总共发了多少张牌？

#### 算法步骤
1.  创建一个变量 `time = 0` 记录时间。
2.  使用一个循环不断遍历数组 `tickets`。
3.  遍历时，如果当前人 `tickets[i] > 0`：
    - 他买一张票：`tickets[i]减1`，`time加1`。
    - **检查**：如果此时 `tickets[k] == 0`，说明目标人物买完了，直接返回 `time`。
4.  重复步骤 2-3，直到满足条件。

#### 复杂度分析
- **时间复杂度**：O(N * max(tickets))。最坏情况下，每个人都买很多票，队伍要转很多圈。但由于数据范围小（N<=100），这个方法完全可行。
- **空间复杂度**：O(1)（如果允许修改原数组）或 O(N)（如果不允许修改，需拷贝一份）。

---

### 方法二：计算贡献（一次遍历优化）

#### 🌟 形象化理解：提前算账

**场景类比**：
不需要真的去排队模拟。你可以直接走到每个人面前问：“在 k 走之前，你会买几张票？”

假设目标人物 **k** 需要买 **3** 张票：
- **路人 A**（排在 k 前面）：
    - 如果 A 只想买 1 张 -> 他会买 1 张就走，贡献 1 秒。
    - 如果 A 想买 10 张 -> 他最多只能买 3 张（因为 k 买第 3 张时就结束了），贡献 3 秒。
    - **结论**：排在 k 前面的人，贡献时间 = `min(A的需求, k的需求)`。

- **路人 B**（排在 k 后面）：
    - 如果 B 只想买 1 张 -> 他会买 1 张就走，贡献 1 秒。
    - 如果 B 想买 10 张 -> 他最多只能买 2 张！
    - **为什么是 2 张？** 因为当 k 买第 3 张（最后一张）票时，还没轮到 B，k 就已经买完宣布结束了。
    - **结论**：排在 k 后面的人，贡献时间 = `min(B的需求, k的需求 - 1)`。

#### 算法步骤
1.  获取目标人物需要的票数 `target = tickets[k]`。
2.  遍历数组中每个人 `i`：
    - 如果 `i <= k`（在 k 前面或就是 k）：累加 `min(tickets[i], target)`。
    - 如果 `i > k`（在 k 后面）：累加 `min(tickets[i], target - 1)`。
3.  累加的结果就是总时间。

#### 复杂度分析
- **时间复杂度**：**O(N)**。只需要遍历一次数组。
- **空间复杂度**：**O(1)**。只需要常数个变量。

---

## 🎨 图解说明 (方法二逻辑)

假设 `tickets = [2, 3, 2]`, `k = 2` (即最后那个人，需求为2)。

| 人员下标 | 需要票数 | 相对 k 的位置 | 逻辑分析 | 贡献时间 |
| :--- | :--- | :--- | :--- | :--- |
| 0 | 2 | 前面 (`i < k`) | k 买 2 张时，他也能买 2 张 (2 <= 2) | `min(2, 2) = 2` |
| 1 | 3 | 前面 (`i < k`) | k 买 2 张时，他最多买 2 张就结束了 | `min(3, 2) = 2` |
| **2 (k)** | **2** | **自己** | **必须买满 2 张** | **`min(2, 2) = 2`** |
| **总计** | | | | **6 秒** |

假设 `tickets = [5, 1, 1, 1]`, `k = 0` (第一个人，需求为5)。

| 人员下标 | 需要票数 | 相对 k 的位置 | 逻辑分析 | 贡献时间 |
| :--- | :--- | :--- | :--- | :--- |
| **0 (k)** | **5** | **自己** | **必须买满 5 张** | **`min(5, 5) = 5`** |
| 1 | 1 | 后面 (`i > k`) | k 买完第 5 张就走了，这轮轮不到他。但他只需要 1 张，在前几轮肯定买完了。 | `min(1, 4) = 1` |
| 2 | 1 | 后面 (`i > k`) | 同上 | `min(1, 4) = 1` |
| 3 | 1 | 后面 (`i > k`) | 同上 | `min(1, 4) = 1` |
| **总计** | | | | **8 秒** |

---

## ✏️ 代码框架填空

> **💡 学习提示**：尝试用 **方法二（一次遍历）** 的思路来完成填空。

### Python填空版

```python
def timeRequiredToBuy(tickets: List[int], k: int) -> int:
    total_time = 0
    # 获取第 k 个人需要买的票数
    target_tickets = tickets[k]
    
    for i, num in enumerate(tickets):
        # 🔹 填空1：处理排在 k 前面（包括 k 自己）的人
        if i <= k:
            # 提示：他们最多买 target_tickets 张，或者买 num 张（看谁小）
            total_time += min(______, ______) num, target_tickets # answer 1
        # 🔹 填空2：处理排在 k 后面的人
        else:
            # 提示：在最后一轮，k 买完就结束了，轮不到后面的人
            # 所以他们最多只能陪跑 target_tickets - 1 轮
            total_time += min(______, ______) num, target_tickets-1 # answer 2
            
    return total_time
```

### C++填空版

```cpp
int timeRequiredToBuy(vector<int>& tickets, int k) {
    int total_time = 0;
    int target = tickets[k];
    
    for (int i = 0; i < tickets.size(); ++i) {
        if (i <= k) {
            // 🔹 填空1：k 前面的人（含 k）
            // 提示：比较 tickets[i] 和 target
            total_time += min(______, ______); target, tickets[i] // answer 1
        } else {
            // 🔹 填空2：k 后面的人
            // 提示：比较 tickets[i] 和 target - 1
            total_time += min(______, ______); target-1, tickets[i] // answer 2
        }
    }
    
    return total_time;
}
```

---

## 💻 完整代码实现

> **✅ 对照检查**

### Python实现 (一次遍历)

```python
class Solution:
    def timeRequiredToBuy(self, tickets: List[int], k: int) -> int:
        """
        计算买票所需时间 - 一次遍历优化法
        时间复杂度: O(N)
        空间复杂度: O(1)
        """
        total_time = 0
        # 目标人物 k 需要的票数
        target = tickets[k]
        
        for i, t in enumerate(tickets):
            if i <= k:
                # 情况1：排在 k 前面（或就是 k）
                # 只要 k 还没买完，这些人都在买。
                # 他们最多能买 target 张。如果他们需要的少于 target，那就只买 t 张。
                total_time += min(t, target)
            else:
                # 情况2：排在 k 后面
                # 当 k 买第 target 张票时（这是 k 的最后一轮），k 离开后游戏立刻结束。
                # 所以这些人这一轮没机会买票。他们最多只能买 target - 1 张。
                total_time += min(t, target - 1)
                
        return total_time

# 测试用例
if __name__ == "__main__":
    sol = Solution()
    print(sol.timeRequiredToBuy([2, 3, 2], 2)) # 预期: 6
    print(sol.timeRequiredToBuy([5, 1, 1, 1], 0)) # 预期: 8
```

### C++实现 (模拟法 & 一次遍历法)

```cpp
#include <vector>
#include <algorithm>
#include <iostream>

using namespace std;

class Solution {
public:
    // 方法一：模拟法（直观，适合面试快速实现）
    int timeRequiredToBuy_Simulation(vector<int>& tickets, int k) {
        int time = 0;
        int n = tickets.size();
        while (tickets[k] > 0) { // 只要 k 还没买完
            for (int i = 0; i < n; ++i) {
                if (tickets[k] == 0) break; // k 买完了，直接退出
                if (tickets[i] > 0) {
                    tickets[i]--;
                    time++;
                }
            }
        }
        return time;
    }

    // 方法二：计算法（最优解）
    int timeRequiredToBuy(vector<int>& tickets, int k) {
        int total = 0;
        int target = tickets[k];
        for (int i = 0; i < tickets.size(); ++i) {
            if (i <= k) {
                // 在 k 前面的人，最多买 target 张
                total += min(tickets[i], target);
            } else {
                // 在 k 后面的人，最多买 target - 1 张
                total += min(tickets[i], target - 1);
            }
        }
        return total;
    }
};
```

---

## ⚠️ 易错点提醒

### 1. 边界条件理解
**易错点**：对于排在 `k` 后面的人，忘记减 1。
- **原因**：容易想当然地认为大家机会均等。实际上，最后一轮循环是“不完整”的，到 `k` 为止就截断了。

### 2. min 函数的使用
**易错点**：直接加上 `tickets[i]`。
- **原因**：忽略了有人可能只需要 1 张票就走了，不会一直陪跑到第 `target` 轮。必须取 `min`。

---

## 🔗 相似题目推荐

### 模拟类题目
1.  **1700. 无法吃午餐的学生数量** (Easy)
    - 相似点：也是队列模拟，如果不满足条件就去队尾。
2.  **1823. 找出游戏的获胜者** (Medium)
    - 相似点：约瑟夫环问题，经典的循环模拟。

### 数组逻辑题
1.  **238. 除自身以外数组的乘积** (Medium)
    - 相似点：通过一次或两次遍历，计算每个位置对他人的“贡献”或“影响”。

## 📚 知识点总结

### 核心算法
- **模拟 (Simulation)**：将题目描述的过程翻译成代码，按部就班执行。
- **贪心/数学推导**：通过观察规律，跳过模拟过程直接计算结果，通常能将复杂度从 O(N^2) 降至 O(N)。

### 学习要点
1.  对于“循环队列”或“轮次”问题，先思考是否可以直接计算轮次贡献。
2.  区分“目标位置之前”和“目标位置之后”的处理逻辑差异。
